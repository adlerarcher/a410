/**
 * Tailwind CSS v4: navy, gold, and workshop tokens live in `src/styles/global.css` (`@theme`).
 * Content paths here keep tooling and any future `@config` usage aligned with the Astro layout.
 */
export default {
	content: ['./src/**/*.{astro,html,js,jsx,ts,tsx,md,mdx}']
};
